Fix the mobile phone experience for the Quran tracing app. The app now works much better on iPad, but the mobile/phone version still needs major responsive improvements.

The goal:
Make the app feel clean, readable, touch-friendly, and properly scaled on ALL phone sizes.

IMPORTANT:
- Do NOT ruin the current iPad layout
- Do NOT shrink the Quran text again
- Only optimize the mobile version

========================
1. FULL MOBILE RESPONSIVENESS
========================

The app must properly support:
- iPhone SE
- Standard iPhones
- iPhone Plus/Max
- Android phones
- Small screens
- Large screens

Requirements:
✅ No overflow  
✅ No cut off text  
✅ No overlapping UI  
✅ No squished layout  
✅ Proper scaling on all devices  

Use proper responsive breakpoints.

========================
2. MAKE QURAN TEXT BIGGER ON MOBILE
========================

Current issue:
- Quran text is too small on phones
- Hard to trace/read comfortably

Fix:
- Increase Arabic text size significantly on mobile
- Use responsive scaling

Example:
font-size: clamp(30px, 7vw, 52px);

Requirements:
✅ Large readable text  
✅ Easy tracing  
✅ Proper spacing between lines  
✅ No clipped Arabic letters  

========================
3. KEEP TEXT INSIDE SCREEN
========================

Current issue:
- Larger text may overflow or get cut off

Fix:
- Ensure text always fits inside screen width
- Proper RTL wrapping
- Proper container sizing
- Responsive padding

Requirements:
✅ Text stays inside screen  
✅ No horizontal scrolling  
✅ No cut-off letters  
✅ Clean wrapping  

========================
4. MOBILE CONTROL SYSTEM
========================

On phones:
- Hide permanent sidebars/settings
- Use floating buttons instead

Controls should:
- Open as popups/modals/drawers
- Close cleanly
- NOT cover entire Quran unnecessarily

Requirements:
✅ Compact controls  
✅ Easy touch targets  
✅ Beginner-friendly UI  
✅ Minimal clutter  

========================
5. MOBILE TRACING
========================

Ensure tracing still works properly on phones.

Requirements:
✅ Finger tracing works  
✅ Draw Mode toggle works  
✅ Smooth tracing  
✅ No lag  
✅ No accidental scrolling while drawing  
✅ Smooth scrolling when not drawing  

========================
6. MOBILE SCROLLING
========================

Fix scrolling for all phones.

Requirements:
✅ Smooth vertical scrolling  
✅ Momentum scrolling  
✅ No freezing  
✅ No blocked scrolling  
✅ Works in Safari and Chrome  

Use:
overflow-y: auto;
-webkit-overflow-scrolling: touch;

========================
7. MOBILE SPACING + LAYOUT
========================

Fix spacing issues on small screens.

Requirements:
✅ Proper padding  
✅ No giant empty spaces  
✅ Balanced margins  
✅ Header fits properly  
✅ Surah title centered  

The Quran content should remain the focus of the screen.

========================
8. RESPONSIVE DESIGN RULES
========================

Desktop:
- Larger layout
- Full controls

iPad:
- Optimized tracing layout
- Larger text
- Apple Pencil support

Phone:
- Fullscreen Quran focus
- Floating controls only
- Larger mobile text
- Simplified UI

========================
9. FINAL MOBILE QA TEST
========================

Confirm ALL work correctly on phones:

✅ Quran text larger  
✅ Text fits inside screen  
✅ No horizontal overflow  
✅ No cut-off Arabic letters  
✅ Floating controls work  
✅ Finger tracing works  
✅ Scrolling works smoothly  
✅ No overlapping UI  
✅ No squished layout  
✅ Works on small phones  
✅ Works on large phones  

The mobile experience should feel polished, simple, responsive, and easy for younger users to use