Fix the tracing latency/input delay issue in the Quran tracing app.

Current issue:
- The trace/stroke appears a few seconds AFTER the user draws.
- This makes tracing feel delayed, laggy, and unusable.

Tracing must appear INSTANTLY in real time.

========================
1. FIX INPUT LATENCY (CRITICAL)
========================

The drawing engine must render strokes immediately while the user is drawing.

Current problem:
- Rendering is delayed
- Stroke updates happen too slowly
- Input feels disconnected from the pen/finger

Fix:
- Render strokes in real time
- Update canvas immediately on pointer movement
- Eliminate delayed batching/rendering

Requirements:
✅ Instant stroke rendering  
✅ No visible delay  
✅ Real-time tracing feedback  
✅ Smooth handwriting feel  

========================
2. OPTIMIZE DRAWING PERFORMANCE
========================

Likely issue:
- Entire canvas or page is re-rendering too often
- React state updates are too expensive
- Drawing data stored inefficiently

Fix:
- Avoid full React re-renders on every stroke
- Use refs instead of state for live drawing data
- Draw directly to canvas context during movement

IMPORTANT:
- Do NOT store every movement in expensive UI state updates

========================
3. USE requestAnimationFrame
========================

Optimize rendering loop.

Use:
requestAnimationFrame()

Purpose:
- Smooth rendering
- Better frame timing
- Lower input lag

Requirements:
✅ Smooth frame updates  
✅ Consistent drawing speed  
✅ Better Pencil responsiveness  

========================
4. REDUCE CANVAS OVERHEAD
========================

Fix:
- Avoid clearing/redrawing entire canvas constantly
- Only draw newest stroke segments
- Minimize expensive calculations during movement

Requirements:
✅ Lightweight drawing  
✅ Stable FPS  
✅ No stuttering  

========================
5. OPTIMIZE APPLE PENCIL PERFORMANCE
========================

Apple Pencil should feel:
- Instant
- Smooth
- Low latency
- Natural

Fix:
- Prioritize pointerType === "pen"
- Reduce smoothing delay
- Use high-frequency pointer updates if available

========================
6. FIX FINGER DRAWING PERFORMANCE
========================

Finger tracing should also:
- Render instantly
- Feel responsive
- Not lag behind movement

========================
7. KEEP CURRENT FEATURES
========================

DO NOT break:
✅ Draw Mode toggle  
✅ Scrolling  
✅ iPad layout  
✅ Mobile layout  
✅ Current text sizing  
✅ Current controls  

Only improve tracing responsiveness/performance.

========================
8. FINAL QA TEST
========================

Confirm ALL work correctly:

✅ Trace appears instantly  
✅ No delayed strokes  
✅ Apple Pencil feels responsive  
✅ Finger tracing feels responsive  
✅ Smooth continuous lines  
✅ No lag spikes  
✅ No freezing  
✅ No delayed rendering after lifting pen  
✅ Scrolling still works properly  

The tracing experience should now feel like a real professional handwriting app with immediate 